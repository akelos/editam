<?php
    $search_replace = array(
            array(
                "searched" => "/(authentication_url:\s*controller: dashboard\s*module: admin)/",
                "detect_modified" => "/authentication_url:\s*controller: page\s*module: editam/",
                "replaced" => "authentication_url:
      controller: page
      module: editam"
            )
    );
?>